# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# UV
#   Similar to Pipfile.lock, it is generally recommended to include uv.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#uv.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/latest/usage/project/#working-with-version-control
.pdm.toml
.pdm-python
.pdm-build/

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Ruff stuff:
.ruff_cache/

# PyPI configuration file
.pypirc

# Cursor  
#  Cursor is an AI-powered code editor.`.cursorignore` specifies files/directories to 
#  exclude from AI features like autocomplete and code analysis. Recommended for sensitive data
#  refer to https://docs.cursor.com/context/ignore-files
.cursorignore
.cursorindexingignore
# The whole data/ directory: large third-party inputs (NJDOT's statewide roadway
# GeoJSON, Mercer County parcels, SLD PDFs), the indexed copies built from them by
# scripts/convert_road_network.py, and any working screenshots. README's "Data" section
# already describes data/ as gitignored - it was previously a per-FILE list, which is
# fragile and had already let 22.5 MB of ad-hoc screenshots into the repo. A directory
# rule can't miss a new file the way a filename list does.
data/
# ...except the committed clip of it. `data/` above matches a directory of that name at ANY
# depth, so it swallowed tests/fixtures/data too - and that clip is the only reason CI can build
# a real junction at all (scripts/make_data_fixture.py, ~5 MB, kilobytes per site plus parcel
# polygons). Re-including the directory is enough; nothing else here matches its contents.
!tests/fixtures/data/

# Downloaded/derived caches under output/. .textures is 138 MB of Poly Haven CC0
# textures and glTF models fetched by src/render/theme.py:build_default_theme(), and
# .cache is Overpass responses - both are re-downloaded on demand and neither belongs
# in the repo. The renders and geometry JSON in output/<site>/ ARE committed; these two
# directories are not.
output/.textures/
output/.cache/

# The 3D renders themselves: ~3 MB each and growing by one per proposal per site (60 MB
# across 21 files already). They are regenerable from the committed geometry_*.json by
# scripts/phase4_render_3d.py, so the reconstruction is preserved in git while the
# pictures of it are not. Everything else under output/ - the geometry JSON, the phase 2
# plan views and the phase 3 before/after plots - IS committed: small, diffable, and the
# 2D plan view is the reviewable artifact this project checks its 3D renders against.
output/**/phase4_render_*.png
# Same rule for a slice's 3D still, which render_slice.py --3d writes beside its 2D sheet:
# the sheet and the geometry JSON are committed, the picture built from them is not.
output/slices/*_3d.png

.DS_Store
.DS_Store
output/.DS_Store
output/.DS_Store
.DS_Store

# The before/after export trees and the known-failure baseline scripts/verify.py keeps.
# Derived, per-machine, and rewritten on every run - like .cache and .textures above.
output/.verify/
